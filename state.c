#include <stdio.h>
#include <stdlib.h>

#include "state.h"

State newState(short depth) {
    State state;
    // mallocy a inicializace structu State
    return state;
}

void freeState(State state) {
    // free(state->neco)
}
